package Program;

// WAP to display the last two character in a reverse format.

public class String_Last_Two_Character_Reverse {

	public static void main(String[] args) {
		String str="bat";
		String result=str.charAt(str.length()-1)+" "+str.charAt(str.length()-2);
		System.out.println(result);

	}

}
